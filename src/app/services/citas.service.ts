import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class CitasService {
  private citas: any[] = []; // Brayan reemplazará "any" por la interfaz Cita

  // Brayan agregará aquí: agregarCita, editarCita, eliminarCita,
  // validarChoqueHorario, y el resumen de totales (reto adicional)

  constructor() {}
}
